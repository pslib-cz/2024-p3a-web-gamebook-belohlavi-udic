name: Nasazen� na Sandbox

on:
  release:
    types:
      - released
  workflow_dispatch: {}

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Naklonov�n� repozit�re
        uses: actions/checkout@v4

      - name: Prihl�en� se k Sandboxu
        uses: docker/login-action@v3
        with:
          username: ignored
          password: ${{ secrets.SANDBOX_BUILD_TOKEN }}
          registry: registry.pslib.cloud

      - name: Nainstalov�n� Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Sestaven� a nahr�n� kontejnerov�ho obrazu
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: registry.pslib.cloud/sandbox/app-51:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
